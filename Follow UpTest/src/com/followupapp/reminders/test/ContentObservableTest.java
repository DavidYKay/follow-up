package com.followupapp.reminders.test;

import android.test.InstrumentationTestCase;

public class ContentObservableTest extends InstrumentationTestCase {

	/**
	 * Test if we can properly monitor the SMS ContentProvider.
	 */
	public void testContentObservable() {
		// Mock out the SMS ContentProvider
		// Create a ContentObserver
		// Modify the contentProvider
		// ContentObserver should have fired
	}
}
